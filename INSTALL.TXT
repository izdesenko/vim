mv vim .vim;
ln -s .vim/vimrc .vimrc;
cd .vim;
git submodule init && git submodule update;
git submodule foreach git pull origin master;

# git submodule add git://github.com/...git bundle/... to add submodule
# ln -s .vim/perldb .perldb; # install perl debugger config
# echo "source ~/.vim/bash_include" >> ~/.bashrc # to include bash configs

